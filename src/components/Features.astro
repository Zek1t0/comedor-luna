---
const base = import.meta.env.BASE_URL;
---
<section class="section bg-[#f8efe6] border-y border-line" aria-labelledby="caracteristicas">
  <div class="container-max grid md:grid-cols-3 gap-7">

    <!-- Recetas Familiares — tenedor + cuchillo (tu SVG) -->
    <article data-animate="up" style="--reveal-delay: 0ms" class="grid place-items-center text-center gap-2 p-4">
      <div class="w-16 h-16 rounded-full bg-accent/70 grid place-items-center shadow-inner">
        <img
          src={`${base}icons/fork-and-knife.svg`}
          alt="Tenedor y cuchillo"
          class="w-8 h-8"
          width="24" height="24"
          loading="lazy" decoding="async"
        />
      </div>
      <h3 class="text-xl font-serif">Recetas Familiares</h3>
      <p class="muted max-w-[34ch]">
        Sabores auténticos transmitidos de generación en generación, manteniendo viva la tradición.
      </p>
    </article>

    <!-- Comida Casera — tazón (tu SVG) -->
    <article data-animate="up" style="--reveal-delay: 100ms" class="grid place-items-center text-center gap-2 p-4">
      <div class="w-16 h-16 rounded-full bg-accent/70 grid place-items-center shadow-inner">
        <img
          src={`${base}icons/home-made-minimalist.svg`}
          alt="Tazón humeante"
          class="w-8 h-8"
          width="24" height="24"
          loading="lazy" decoding="async"
        />
      </div>
      <h3 class="text-xl font-serif">Comida Casera</h3>
      <p class="muted max-w-[34ch]">
        Cada plato preparado al momento con ingredientes frescos y el cariño de nuestra cocina.
      </p>
    </article>

    <!-- Servicio Puntual — reloj (SVG inline) -->
    <article data-animate="up" style="--reveal-delay: 200ms" class="grid place-items-center text-center gap-2 p-4">
      <div class="w-16 h-16 rounded-full bg-accent/70 grid place-items-center shadow-inner">
        <svg viewBox="0 0 24 24" width="24" height="24"
             fill="none" stroke="currentColor" stroke-width="2"
             stroke-linecap="round" stroke-linejoin="round"
             class="w-8 h-8 text-primary">
          <circle cx="12" cy="12" r="9" />
          <path d="M12 7v5l3 3" />
        </svg>
      </div>
      <h3 class="text-xl font-serif">Servicio Puntual</h3>
      <p class="muted max-w-[34ch]">
        Disfrutá tu comida en el momento preciso, con la calidez que merecés.
      </p>
    </article>

  </div>
</section>
