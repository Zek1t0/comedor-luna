---
const path = Astro.url.pathname;
const link = (href: string) =>
  `opacity-85 hover:opacity-100 ${path.startsWith(href) ? 'font-semibold text-primary opacity-100' : ''}`;
---

<nav class="sticky top-0 z-50 border-b border-black/10 bg-white/70 backdrop-blur">
  <div class="container-max flex items-center justify-between py-3">
    <a href="#top" class="font-serif text-2xl">Comedor Luna</a>
    <div class="flex items-center gap-5">
      <a href="#menu" class="opacity-85 hover:opacity-100">Menú</a>
      <a href="#historia" class="opacity-85 hover:opacity-100">Nuestra Historia</a>
      <a href="#contacto" class="opacity-85 hover:opacity-100">Contacto</a>
      <a href="#complementos" class="btn btn-primary">Complementos</a>
    </div>
  </div>
</nav>