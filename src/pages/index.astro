---
import BaseLayout from '../layouts/BaseLayout.astro';
import NavBar from '../components/NavBar.astro';
import Features from '../components/Features.astro';
import Testimonials from '../components/Testimonials.astro';
import Menu from '../components/Menu.astro';
import Contact from '../components/Contact.astro';
import Footer from '../components/Footer.astro';
import WhatsAppButton from '../components/react/WhatsappButton.tsx';
---
<BaseLayout title="Comedor Luna – Sabores que cuentan historias familiares">
  <a id="top"></a>
  <NavBar />

<header
  class="relative grid place-items-center min-h-[92vh] md:min-h-[96vh] pt-6 hero"
  style="--hero-image: url('/hero1.png')"
>
<div class="absolute inset-0 bg-gradient-to-r from-black/85 via-black/75 to-black/85"></div>
  <div class="relative text-white text-center py-[6vh] container-max" data-animate>
    <h1 class="max-w-4xl mx-auto font-serif text-[clamp(2.4rem,5vw,4.5rem)] leading-[1.05] drop-shadow-[0_4px_18px_rgba(0,0,0,.6)]">
      Sabores que cuentan historias familiares
    </h1>
    <p class="max-w-[780px] mx-auto opacity-90 text-white mt-3 drop-shadow-[0_2px_10px_rgba(0,0,0,.55)]">
      Desde 1962, sirviendo los sabores auténticos de la cocina casera, transmitidos de generación en generación con el mismo amor de siempre.
    </p>

    <div class="flex flex-wrap gap-3 justify-center mt-6">
      <a class="btn btn-primary shadow-lgx" href="#menu">Ver Nuestro Menú</a>
      <a
        class="btn border border-white/30 text-white hover:bg-white/10"
        href="#historia"
      >
        Nuestra Historia
      </a>
    </div>
  </div>
</header>

  <Features />
  <Testimonials />
  <Menu />

  <section id="historia" class="section">
    <div class="container-max">
      <h2 class="section-title">Nuestra Historia</h2>
      <p class="muted-center">Desde 1962, Comedor Luna es un punto de encuentro para familias y amigos en Sáenz Peña. Nacimos como una mesa larga en casa y crecimos conservando la sencillez y el sabor casero.</p>
    </div>
  </section>

  <Contact />
  <Footer />
  <WhatsAppButton client:visible />

  <style>
    .hero::before{
      content:"";
      position:absolute;
      inset:0;
      background:
        var(--hero-image);
      background-size:cover;
      background-position:center;
    }
  </style>
</BaseLayout>