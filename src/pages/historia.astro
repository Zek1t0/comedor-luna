---
import BaseLayout from "../layouts/BaseLayout.astro";
import NavBar from "../components/NavBar.astro";
import Footer from "../components/Footer.astro";
const base = import.meta.env.BASE_URL;

const features = [
  {
    icon: "🍽️",
    title: "Sabores Auténticos",
    desc:
      "Cada plato se prepara con recetas tradicionales e ingredientes frescos, manteniendo vivos los sabores desde el primer día."
  },
  {
    icon: "🏠",
    title: "Ambiente Familiar",
    desc:
      "Nuestro espacio está pensado para que te sientas como en casa. Cada mesa invita a compartir momentos especiales."
  },
  {
    icon: "❤️",
    title: "Servicio del Corazón",
    desc:
      "Más que servir comida, creamos experiencias. Nos dedicamos a que cada visita sea memorable."
  },
  {
    icon: "✨",
    title: "Tradición Viva",
    desc:
      "Honramos nuestras raíces mientras evolucionamos, manteniendo la esencia que nos hace únicos."
  },
];
---

<BaseLayout title="Nuestra Historia – Comedor Luna">
  <NavBar />
  <!-- HERO con gradiente + fade ultra smooth -->
    <header
      class="
        relative isolate overflow-clip text-white
        min-h-[62svh] grid place-items-center
        px-4
        bg-[radial-gradient(1200px_720px_at_50%_-20%,rgba(240,106,0,.18),transparent_60%),linear-gradient(180deg,#5a2f25_0%,#4b2b21_42%,#3d211a_72%,#2f241c_100%)]
        [mask-image:linear-gradient(180deg,rgba(0,0,0,1)_0%,rgba(0,0,0,1)_76%,rgba(0,0,0,0)_100%)]
      "
    >
      <div class="container-max text-center py-24 md:py-36">
        <h1 class="font-serif text-4xl md:text-6xl tracking-tight drop-shadow-sm">
          Nuestra Historia
        </h1>
        <p class="mt-6 text-lg md:text-xl opacity-90 max-w-[65ch] mx-auto">
          Desde 1962, creando momentos especiales alrededor de la mesa con sabores auténticos y calidez humana.
        </p>
      </div>
    </header>

    <!-- Sección: El Comienzo -->
    <section class="section">
      <div class="container-max">
        <h2 class="section-title">El Comienzo</h2>
        <div class="h-1 w-16 bg-primary/50 mx-auto rounded-full mb-10"></div>

        <div class="grid items-center gap-10 md:gap-14 md:grid-cols-[1fr,520px]">
          <div>
            <p class="text-4xl md:text-5xl font-serif text-primary mb-4">1962</p>
            <h3 class="text-2xl md:text-3xl font-serif mb-4">Un Sueño Hecho Realidad</h3>
            <div class="space-y-4 text-lg text-muted">
              <p>
                En el corazón de nuestra ciudad, nació Comedor Luna con una visión simple pero poderosa:
                crear un lugar donde familias y amigos pudieran reunirse alrededor de una mesa, compartiendo
                no solo comida, sino momentos que perduran en la memoria.
              </p>
              <p>
                Desde ese primer día, mantenemos viva la pasión por la cocina casera, los ingredientes
                frescos y el servicio que hace sentir a cada cliente como en casa.
              </p>
            </div>
          </div>

          <figure class="rounded-xl2 overflow-clip shadow-lgx">
            <img
              src="https://images.unsplash.com/photo-1528605248644-14dd04022da1?q=80&w=1400&auto=format&fit=crop"
              alt="Comedor de época con luces cálidas"
              class="w-full h-auto"
              loading="lazy"
            />
          </figure>
        </div>
      </div>
    </section>

    <!-- Sección: Lo que nos define -->
    <section class="section pt-8">
      <div class="container-max">
        <h2 class="section-title">Lo Que Nos Define</h2>
        <p class="muted-center">
          Los elementos que hacen de Comedor Luna un lugar especial donde cada comida es una experiencia memorable.
        </p>

        <div class="grid gap-6 md:gap-8 md:grid-cols-2">
          {features.map((f) => (
            <article
              class="
                rounded-xl2 bg-accent/40 border border-line/60 p-7 md:p-8 shadow-mdx
                transition duration-300 hover:shadow-lgx
              "
            >
              <div class="text-3xl mb-3">{f.icon}</div>
              <h3 class="text-xl font-semibold mb-2">{f.title}</h3>
              <p class="text-muted leading-relaxed">{f.desc}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="py-16">
      <div class="container-max">
        <div class="rounded-xl2 shadow-lgx p-10 md:p-12 text-cream bg-[var(--color-footer)]">
          <h3 class="font-serif text-2xl md:text-3xl mb-4">Ven a Vivir la Experiencia</h3>
          <p class="max-w-[70ch] opacity-90">
            Descubrí por qué Comedor Luna es más que un restaurante: es el lugar donde los sabores auténticos
            se encuentran con momentos inolvidables.
          </p>

          <div class="mt-8 flex flex-wrap gap-4">
            <a href={`${base}#contacto`} class="btn btn-primary">Reservar Mesa</a>
            <a href={`${base}menu/`} class="btn btn-ghost">Ver Menú</a>
          </div>
        </div>
      </div>
    </section>
    <!-- Footer -->
     <Footer />
</BaseLayout>